// Generated by CoffeeScript 1.6.3
(function() {
  var _base, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.RTC || (window.RTC = {});

  (_base = window.RTC).Peer || (_base.Peer = {});

  window.RTC.Peer.Remote = (function(_super) {
    __extends(Remote, _super);

    function Remote() {
      _ref = Remote.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    Remote.prototype.connect = function(local, success, failure) {
      var self;
      if (success == null) {
        success = null;
      }
      if (failure == null) {
        failure = null;
      }
      Logger.trace('RTC.Peer.Remote->connect');
      self = this;
      this.localConnection = new RTC.Connection.Local(local.name);
      this.remoteConnection = new RTC.Connection.Remote(this.name);
      this.localConnection.createOffer(function(desc) {
        return self.gotLocalDescription(desc, success, failure);
      });
      return this;
    };

    Remote.prototype.gotLocalDescription = function(desc, success, failure) {
      var self;
      if (success == null) {
        success = null;
      }
      if (failure == null) {
        failure = null;
      }
      Logger.trace('RTC.Peer.Remote->gotLocalDescription');
      Logger.log(desc.sdp);
      self = this;
      this.localConnection.setLocalDescription(desc);
      this.remoteConnection.setRemoteDescription(desc);
      return this.remoteConnection.createAnswer(function(answer) {
        return self.gotRemoteDescription(answer, success, failure);
      });
    };

    Remote.prototype.gotRemoteDescription = function(desc, success, failure) {
      if (success == null) {
        success = null;
      }
      if (failure == null) {
        failure = null;
      }
      Logger.trace('RTC.Peer.Remote->gotRemoteDescription');
      Logger.log(desc.sdp);
      this.remoteConnection.setLocalDescription(desc);
      this.localConnection.setRemoteDescription(desc);
      return success();
    };

    Remote.prototype.send = function(message) {
      Logger.trace('RTC.Peer.Remote->send');
      Logger.log(message);
      return this.remoteConnection.send(message);
    };

    return Remote;

  })(window.RTC.Peer.Peer);

}).call(this);
